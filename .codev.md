# CO-DEV

Gate: normal
Ceremony: light
Execution engine: superpower
Current gate: readme-default-normal-light
Decision: pending
Decision gate: readme-default-normal-light

## Intent

Keep the CO-DEV v0.3 documentation, skills, templates, and tests aligned on the default governance posture.

## Shape

Current batch: align the README recommendation with the `using-codev` skill and starter template.

Next gate: inspect the documented default and structure-test evidence before publication.

## Trace

- CodeV explicit activation: require `.codev.md` plus `using-codev` before execution.
- CodeV status command: print the active governance state.
- CodeV repository architecture: document governance versus execution responsibilities.
- Integrity release started: add gate-bound decisions, complete validation, and cross-platform verification.
- v0.3 integrity release implemented with 65 gate tests, v0.3 structure tests, four manifests at 0.3.0, cross-platform CI plus pinned official manifest validation configured, pending human gate integrity-release-v0.3.
- Final integrity review fixed case-insensitive command dispatch, Markdown fence and heading isolation, atomic approval replacement, actionable pre-v0.3 migration guidance, and CI validator coverage.
- Focused rereview added Setext H2 isolation, concurrent-edit conflict restoration, Unix permission/ACL preservation, and real PowerShell 7 verification on Windows and Linux.
- Recovery hardening follows superseding edits atomically, preserves concurrent Unix chmod changes, and removes the arbitrary-file ready hook.
- Unix publication now uses native atomic exchange, fingerprints ACL/xattr metadata, covers editor-style atomic saves, and validates recovery hooks before publication.
- Fresh review hardening adds ordinal gate identity, canonical `none`, strict decoding for every command, and first-publication snapshot continuity through Unix metadata synchronization.
- Final token hardening validates fixed enums and command branches ordinally, rejecting Unicode-ignorable gate and approval aliases.
- Documentation drift corrected: README now recommends `normal + light`, matching `using-codev` and the starter template; structure coverage updated, pending gate `readme-default-normal-light`.
